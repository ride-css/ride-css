function Gradient (stops) {
  var self = this;

  stop.forEach(function (stop, stopPosition) {
    self[stopPosition] = stop;
  });
}

function GradientLinear (GradientInstance, direction) {
  this.gradient = GradientInstance;
}

function GradientRadial (GradientInstance) {
  this.gradient = GradientInstance;
}

module.export = function () {
  
}